# 仓库维护约定

- 本仓库保存 SVG 绘制评测作品，没有应用入口或已配置的构建、测试、lint 命令。
- README 提示词中的“严禁读取项目内其他文件”是生成评测的隔离要求，不是文档维护的读取限制；执行生成评测时遵守该要求。
- 既有 SVG 是评测产物，维护文档时不要顺手重绘或格式化它们。

## README 对比表

- 表格采用三列结构：「模型名称 • 思考等级」、「Agent」、「作品预览」；`Max` 显示为 `max`。
- 文件名与表格展示统一为规范写法：模型名称与目录名一致（小写）、思考等级小写（如 `high`、`max`、`xhigh`）、Agent 按官方大小写（`Devin`、`Codex`、`Grok`、`DSH`、`pi`、`OpenCode`）；`devin-cli`、`devin` 一律统一为 `Devin`。
- 新增或修正文件名时，同步更新 README 中所有图片与链接引用；表格路径必须指向真实文件。
- 每个作品使用相对路径 SVG 图片预览（宽 256px）并链接原图；新增作品时同步表格。
- 「提示词」位于「原版视频与作品对比」之前；视频封面链接 MP4，「（点击封面打开视频）」须为可见文字，不能仅放在图片 alt 中。
- 修改表格后核对所有 SVG 均被收录、归属正确、图片及目标链接存在；没有现成自动化测试替代这些检查。

## 授权

- MIT 协议不覆盖参考视频及其封面预览；维护文档时保留 README 中的第三方素材授权边界。
